# Database
POSTGRES_USER=your_postgres_username
POSTGRES_PASSWORD=your_postgres_password
POSTGRES_DB=your_database_name

# Application
API_KEY=your_admin_api_key

# Pipeline (set by CI/CD, not manually)
IMAGE_TAG=latest